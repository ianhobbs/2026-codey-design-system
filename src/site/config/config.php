<?php

/**
 * Kirby config — STARTER. Compiled src/site → build/site by CodeKit, so Kirby
 * reads this at build/site/config/config.php. Set project options here.
 */

return [
    // Local development conveniences — turn OFF for production.
    'debug'   => true,
    'panel'   => [
        'install' => true,   // allow creating the first Panel account at /panel
    ],
];
