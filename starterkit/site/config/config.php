<?php
/**
 * Starterkit Kirby config.
 *
 * Site-specific settings only (domain, license, keys go in per-host config
 * files). The design system plugin is loaded automatically via Composer once
 * `ianhobbsmedia/codey-design-system` is required.
 */
return [
    'debug' => true,
    // 'languages' => true,
];
